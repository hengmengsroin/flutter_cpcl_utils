# Changelog

## 0.1.0

- Replace the default package template with a usable CPCL command generator.
- Add chainable APIs for label setup, text, line, box, barcode, QR code, bitmap, and print commands.
- Add Flutter-rendered bitmap text support for Khmer and other Unicode text.
- Document the package and add tests and example usage.
